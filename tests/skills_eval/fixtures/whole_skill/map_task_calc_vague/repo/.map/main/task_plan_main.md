# Task Plan (branch: main)

## Subtasks

### ST-001 — Implement arithmetic evaluate()

Implement `evaluate(expr)` in `src/calc.py` so it correctly evaluates arithmetic
expressions with standard precedence. Make `tests/test_calc_basic.py` pass.

- affected_files: `src/calc.py`
- validation: `python -m pytest tests/test_calc_basic.py -q` passes
