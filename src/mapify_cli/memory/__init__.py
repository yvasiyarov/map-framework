"""mapify_cli.memory — session digest capture and recall subsystem.

Submodules:
  digest_schema  — single-source field-name constants, redaction patterns,
                   and sanitization utilities (pure stdlib, no I/O).
"""
