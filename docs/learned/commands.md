# Command Patterns (Learned)

<!-- IMPROVEMENT-PLAN-LOOP: promoted from loop learnings. Edit freely and commit with the project. -->

- **Use repo-built init for template verification** (2026-04-13): When validating shipped MAP templates, always run `uv run mapify init <new-dir> --no-git --mcp none` because the globally installed `mapify` binary may still reflect an older release instead of the branch you are reviewing. [workflow: improvement-plan-loop]
- **Use non-existent target paths for init smoke tests** (2026-05-15): `mapify init <path>` creates the target directory, so do not pre-create it with `mktemp -d`; pass a unique path under an existing temp parent and then inspect the generated `.claude/` artifacts. [workflow: improvement-plan-loop]
- **Grep stale skill names during taxonomy changes** (2026-05-15): When skill catalog docs change, search for removed or non-shipped skill names such as `map-workflows-guide` and `map-cli-reference`; generated install smoke tests prove current templates emit metadata, but they do not catch every stale prose reference. [workflow: improvement-plan-loop]
- **Validate run health reports in CI handoff** (2026-05-16): Use `python3 .map/scripts/map_step_runner.py validate_run_health_report [path]` after `write_run_health_report` when a workflow closeout must fail on schema drift, retry overflow, missing verification evidence, or unexplained hook degradation. [workflow: improvement-plan-loop]
- **Close already-shipped active ideas with repo evidence** (2026-05-16): Before implementing an active improvement-plan item, inspect active and done headings, then search runtime, shipped templates, docs, and tests for the core artifact or route names; if the capability already exists, move the exact `[idea_id]` to done with evidence instead of rebuilding it. [workflow: improvement-plan-loop]
- **Check both skill and helper surfaces before review-workflow work** (2026-05-17): For `/map-review` backlog items, inspect `.claude/skills/map-review/SKILL.md`, the shipped template copy, `.map/scripts/map_step_runner.py`, docs, roadmap status, and focused tests before implementation; review capabilities can be split across prompt wiring and helper scripts. [workflow: improvement-plan-loop]
- **Smoke generated evidence references** (2026-05-17): When shipped skill prompts link to new shared references, run `uv run --no-sync mapify init <new-dir> --no-git --mcp none` and inspect generated `.claude/references/` plus the generated skill links because source-template sync alone does not prove installed output. [workflow: improvement-plan-loop]
- **Run JSON prompt-contract guardrails** (2026-05-17): When changing MAP skill prompts that contain `Output JSON with:`, run `pytest tests/test_skills.py::TestEvidenceFirstPromptContracts tests/test_template_sync.py -v` so vague JSON contracts fail in both source and shipped template surfaces. [workflow: improvement-plan-loop]
- **Validate blueprint lineage in generated projects** (2026-05-18): When changing blueprint validator semantics, run `uv run --no-sync mapify init <new-dir> --no-git --mcp none` and exercise the generated `.map/scripts/map_step_runner.py validate_blueprint_contract` on both passing and failing fixtures because source tests alone do not prove installed projects enforce the new gate. [workflow: improvement-plan-loop]
- **Validate hard/soft blueprint constraints** (2026-05-18): Use `python3 .map/scripts/map_step_runner.py validate_blueprint_contract <path-to-blueprint.json>` to fail MAP plans where a hard constraint is not in `coverage_map` or an uncovered soft constraint lacks `tradeoff_rationale`. [workflow: improvement-plan-loop]
- **Report acceptance coverage from branch artifacts** (2026-05-18): Use `python3 .map/scripts/map_step_runner.py build_acceptance_coverage_report` to inspect which `coverage_map` tags have downstream evidence before final review or verification handoff. [workflow: improvement-plan-loop]
- **Validate prior-stage consumption before readiness claims** (2026-05-18): Use `python3 .map/scripts/map_step_runner.py validate_prior_stage_consumption implementation` or `review` when a workflow must prove spec, plan, blueprint, test contract, diff, and verification inputs before closeout. [workflow: improvement-plan-loop]
- **Smoke generated skill policies** (2026-05-18): When changing task-skill orchestration policy, run `uv run --no-sync mapify init <new-dir> --no-git --mcp none` and inspect generated `.claude/skills/*/SKILL.md` for the expected `thinking_policy` and `parallel_tool_policy` lines. [workflow: improvement-plan-loop]
- **Smoke generated prompt tone** (2026-05-18): When changing shipped skill prompt wording, run `uv run --no-sync mapify init <new-dir> --no-git --mcp none` and inspect generated `.claude/skills/` because source prompt tests do not prove installed projects receive the calibrated wording. [workflow: improvement-plan-loop]
- **Smoke generated XML prompt envelopes** (2026-05-19): When changing high-context MAP skill prompts, run `uv run --no-sync mapify init <new-dir> --no-git --mcp none` and inspect generated `.claude/skills/` plus `.claude/references/` because source tests alone do not prove installed projects receive the prompt envelope reference. [workflow: improvement-plan-loop]
- **Run Skill IR audit for provider templates** (2026-05-19): When changing shipped Claude or Codex skill templates, run `PYTHONPATH=src python -m mapify_cli.skill_ir src/mapify_cli/templates/skills src/mapify_cli/templates/codex/skills --format json` because it catches provider-surface drift before `mapify init` installs broken skills. [workflow: improvement-plan-loop]
- **Force prompt-budget truncation in focused tests** (2026-05-19): Use a small accepted override such as `MAP_CONTEXT_BLOCK_BUDGET_TOKENS=200` when testing `build_context_block()` so the budget path is exercised and the generated `<map_context>` remains closed under pressure. Values below the 128-token minimum fall back to the default budget. [workflow: improvement-plan-loop]
- **Build bounded review prompts before fan-out** (2026-05-19): Use `python3 .map/scripts/map_step_runner.py build_review_prompts --budget-tokens <n> --review-preferences "..."` to inspect `/map-review` Monitor/Predictor/Evaluator prompts before launch; generated prompts preserve the review bundle as primary context and clip raw diff first. [workflow: improvement-plan-loop]
- **Inspect Budgeted Review Prompts By Branch** (2026-05-20): When validating generated `/map-review` prompt budgeting, run `python3 .map/scripts/map_step_runner.py build_review_prompts --branch <branch> --budget-tokens <n> --review-preferences "..."` because the helper defaults to the current git branch and may otherwise write `.map/<git-branch>/token_budget.json`. [workflow: improvement-plan-loop]
- **Check compact resume skill lifecycle** (2026-05-20): Use `pytest tests/test_skills.py::TestSkillStructure::test_map_resume_keeps_recovery_skill_body_compact tests/test_template_sync.py -v` after changing `/map-resume` so the active recovery body, supporting reference, and shipped template copy stay aligned. [workflow: improvement-plan-loop]
- **Check compact high-traffic skill bodies** (2026-05-20): When changing `/map-plan`, `/map-efficient`, `/map-check`, or `/map-review`, run `pytest tests/test_skills.py::TestSkillStructure::test_high_traffic_workflow_skills_keep_active_bodies_compact tests/test_template_sync.py -v` because source and shipped template bodies must stay compact and link their bundled references. [workflow: improvement-plan-loop]
- **Check mutation-boundary provider prompts** (2026-05-20): When changing write-capable Actor/fix prompts, run `pytest tests/test_skills.py::TestSkillStructure::test_write_capable_claude_surfaces_have_constraint_first_boundaries tests/test_skills.py::TestSkillStructure::test_write_capable_codex_surfaces_have_mutation_boundaries -v` so installed provider surfaces keep unrelated-edit and dependency-change guardrails. [workflow: improvement-plan-loop]
- **Validate clean retry quarantine** (2026-05-20): Use `python3 .map/scripts/map_step_runner.py validate_retry_quarantine` before a clean Actor retry; use `build_retry_quarantine <subtask_id> <retry_count> "<monitor feedback>"` for non-orchestrated debug flows that still need the same quarantine artifact. [workflow: improvement-plan-loop]
